# Transform U Media Network Corp. — corporate page

One static page (`index.html`), no build step. Replaces the current tumnnews.com React/Supabase/Netlify site with a minimal parent-company landing page.

## Before deploying
- Confirm the Warrior Mandate link — I used `https://warriormandate.substack.com` as a placeholder since I couldn't verify the exact Substack URL. Swap it for the real one before this goes live.
- Swap the TUL Show link (currently `#` with a "New site coming" badge) for the real TUL Show URL once that site is live.
- Add Kinetic PE MIXX as a new card whenever it launches (structure mirrors the existing cards — copy one and edit).

## Deploy
This can replace everything currently on tumnnews.com's Netlify deploy — swap the deployed folder for just this one (`index.html` + the two logo PNGs), or drag-and-drop this folder into https://app.netlify.com/drop for a quick preview first.
